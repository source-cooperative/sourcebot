name: Sourcebot Fix
on:
  issues:
    types: [labeled]
jobs:
  fix:
    if: github.event.label.name == 'sourcebot-fix'
    uses: source-cooperative/sourcebot/.github/workflows/fix.yml@main
    secrets: inherit
